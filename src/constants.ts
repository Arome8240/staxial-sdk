/** Milliseconds per Stacks block (~10 minutes, anchored to Bitcoin) */
export const MS_PER_BLOCK = 10 * 60 * 1_000;

/** Stacks blocks per day (approximately) */
export const BLOCKS_PER_DAY = 144;

/** Stacks blocks per week (approximately) */
export const BLOCKS_PER_WEEK = 1_008;

/** Stacks blocks per month (approximately) */
export const BLOCKS_PER_MONTH = 4_320;

/** Stacks blocks per year (approximately) */
export const BLOCKS_PER_YEAR = 52_560;
